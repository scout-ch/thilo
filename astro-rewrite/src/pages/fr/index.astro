---
import MainLayoutWithToC from '../../layouts/MainLayoutWithToC.astro';
import { getStartPage } from '../../utils/data';
import { getTranslations } from '../../i18n';

const locale = 'fr';
const startPage = await getStartPage(locale);
const t = getTranslations(locale);
---

<MainLayoutWithToC title={startPage.title} lang={locale}>
  <div class="prose max-w-4xl mx-auto">
    <h1 class="text-4xl font-bold mb-6">{startPage.title}</h1>
    <div set:html={startPage.content}></div>
  </div>
</MainLayoutWithToC>
